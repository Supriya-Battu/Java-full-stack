package com.collection;

public class HashSetDemo {
//the values are not in order they store
//can't perform list iterator & can perform iterator
	//hashset can apply null value 
	//can't add duplicates 
	//to get in inserted order we use class Linkedhashset()
	//in generic we apply Treeset
}